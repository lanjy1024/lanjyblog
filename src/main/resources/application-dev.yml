spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lanjyblog?useUnicode=true&zeroDateTimeBehavior=convertToNull&characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true&useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
logging:
  config: classpath:logback-spring.xml
server:
  port: 80
